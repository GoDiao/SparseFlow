# Handoff

## Active Agent Roles

**Board** owns the global project view: synchronizing the repository, tracking
Main Dev and Benchmark progress, maintaining the handoff timeline, checking
cross-workstream interfaces, and recording project-level priorities. Board
does not silently take ownership of implementation work.

The active identity for the current implementation workstream is **[Main Dev]**.
Main Dev owns the model-independent runtime implementation and the current
Qwen3.6 text-only reference integration, including expert location/loading,
cache, prefetch, CPU kernels, KV-cache wiring, and correctness validation.

**[WRITER]** remains a separate workstream for public narrative and editorial
sequencing; Writer does not take ownership of runtime implementation.

**Benchmark** is a separate workstream. It owns CPU experiment design,
baseline measurements, Colibri-style quality evaluation, raw-result schemas,
statistical comparisons, and benchmark reports. Benchmark must not silently
take over runtime implementation; it defines the interfaces and measurements
that Main Dev's runtime must expose.

## Agent Attribution

Every new implementation note, experiment result, design decision, or
handoff update must end with the identity of the agent that performed it. Use
the format `[Board]`, `[Main Dev]`, `[Benchmark]`, or another explicit agent
name. Do not leave newly recorded work unattributed.

This attribution rule is shared by [Board], [Main Dev], [Benchmark], and
[WRITER].

## Current State

SparseFlow is an open-source research project for tiered-memory inference on
sparse expert models (MoE). The project is inspired by
`JustVugg/colibri`, but its core should remain model-independent.

The current experiment target is `Qwen/Qwen3.6-35B-A3B`. The project is not
intended to become a Qwen-only wrapper.

The main hypothesis is:

```text
dense/router/shared weights resident in RAM
        +
routed experts streamed from NVMe/SSD on demand
        +
hot expert cache, heat policy, and prefetch
```

The repository is public at <https://github.com/GoDiao/SparseFlow>.
The default branch is `main`, and the local branch tracks `origin/main`.

## Completed Work

1. Studied `JustVugg/colibri` and documented the modules worth borrowing.
   - The local reference checkout is `_reference/colibri`.
   - The detailed report is `docs/colibri_borrowable_modules.md`.
   - Both are intentionally ignored and are not part of the public repository.

2. Created the project repository and initial implementation plan.
   - GitHub repository: `GoDiao/SparseFlow`
   - Plan: `docs/implementation_plan.md`
   - The plan covers inspect/plan, expert location, lazy loading, cache,
     route simulation, and the future runtime boundary.

3. Implemented the first `inspect` / `plan` tooling in `src/sparseflow/`.
   - `safetensors.py`: header-only safetensors shard index.
   - `classifier.py`: Qwen3.6 tensor classification.
   - `analyze.py`: model footprint and theoretical expert read analysis.
   - `plan.py`: RAM/disk tier planning.
   - `cli.py`: command-line entry points.

4. Added the current CLI commands:

   ```bash
   python -m sparseflow inspect <model>
   python -m sparseflow plan <model> --ram <GB> --ctx <tokens>
   ```

5. Downloaded the Qwen3.6 model through ModelScope.
   - The local copy, when present, is at `model/Qwen3.6-35B-A3B` and is
     ignored by Git.
   - The model contains 26 safetensors files and is about `66.97 GiB`.
   - The model has now also been downloaded to the remote experiment machine.
   - The remote path is
     `/root/workspace/SparceFlow/model/Qwen3.6-35B-A3B`.
   - The workspace filesystem reported about `275.49 GiB` total and
     `196.28 GiB` free at the recorded observation time.

6. Created an initial test suite.
   - `tests/test_analyze_plan.py`
   - Current result: 2 tests passed.

7. Added `.handoff.md` so future work can be resumed from a compact project
   state plus a chronological record of important decisions.

8. Established the Benchmark workstream.
   - Wrote `docs/benchmark_setting.md` with the complete CPU performance and
   quality-eval matrix.
   - Defined CPU Full Resident, Generic Offload, SparseFlow Resident, and
     SparseFlow Streaming comparisons.
   - Adapted the Colibri HellaSwag/ARC/MMLU log-likelihood design for future
     multi-backend evaluation.
   - Separated BF16 storage-policy validation from later INT8/INT4 quality and
     performance experiments.

9. Built the first executable Benchmark framework.
   - Added `benchmarks/run_cpu.py` for CPU Full Resident performance runs.
   - Added `benchmarks/score_choices.py` for Colibri-style choice scoring.
   - Added fixed development manifests under `benchmarks/manifests/`.
   - Added process RSS, page fault, CPU time, process I/O, model hash, Git
     state, and raw token result collection.
   - The runner materializes mmap-backed tensors for a true C1 resident run;
     mmap mode is explicitly marked as a separate comparison.
[Benchmark]

9. Implemented the first `ExpertLocator` in `src/sparseflow/locator.py`. [Main Dev]
   - Maps `(layer, expert_id)` to fused `gate_up_proj` and `down_proj` slices. [Main Dev]
   - Returns shard, absolute file offset, byte length, dtype, and logical shape. [Main Dev]
   - Validates expert count, expert axis, dtype size, tensor byte size, and
     slice bounds before any payload read. [Main Dev]
   - Keeps locating separate from loading and caching. [Main Dev]

10. Added the first expert storage probe commands. [Main Dev]
    - `expert-stat` reports exact fused tensor slices without reading payloads. [Main Dev]
    - `expert-load` reads only those ranges and reports byte counts plus SHA-256. [Main Dev]
    - Added `src/sparseflow/loader.py` and CLI coverage in `tests/test_locator.py`. [Main Dev]

11. Validated the expert probe against the real Qwen3.6 model on the
    experiment machine. [Main Dev]
    - `layer=0, expert=17` resolved successfully. [Main Dev]
    - The expert axis is axis 0 for both fused routed tensors. [Main Dev]
    - Total raw bytes read: `6,291,456` (`6.00 MiB`). [Main Dev]
    - Combined SHA-256: `a5222fdcdff4d504d653e58827d47b8a92f6f8e9663aa48d7738790beaa2572b`. [Main Dev]

12. Implemented `ExpertCache` and `expert-bench`. [Main Dev]
    - Added per-layer LRU caching keyed by `(layer, expert_id)`. [Main Dev]
    - Added reproducible uniform/locality trace generation and JSON replay. [Main Dev]
    - Added cache hit/miss, eviction, logical bytes, loaded bytes, latency, and
      process I/O reporting. [Main Dev]
    - Recorded the first real Qwen3.6 pilot in
      `docs/results/expert_cache_20260713_qwen36_pilot.md`. [Main Dev]

13. Added long-trace comparison and real Qwen3.6 route-trace replay. [Main Dev]
    - Compared uniform and locality traces at 0/4/8/16/32 slots per layer. [Main Dev]
    - Added `route-trace`, which captures actual `selected_experts` from the
      Transformers Qwen3.5-MoE router. [Main Dev]
    - Replayed a real trace with 2880 requests and 1577 unique layer/expert
      pairs through `expert-bench`. [Main Dev]
    - Recorded details in
      `docs/results/expert_cache_20260713_long_comparison.md`. [Main Dev]

14. Upgraded route traces to schema v2 and added phase-aware batch-union replay. [Main Dev]
    - Preserves forward, phase, row, token position/id, layer, and expert IDs. [Main Dev]
    - Added `route-trace-batch` for multi-category 8/16/32-token captures. [Main Dev]
    - Added per-phase cache metrics and real prefill/decode result records. [Main Dev]
    - Results: `docs/results/route_trace_v2_long_capture_20260714.md`. [Main Dev]

15. Added single-expert resident/streaming kernel correctness and persistent
    shard readers. [Main Dev]
    - `expert-kernel-check` uses one shared BF16 expert kernel for both paths. [Main Dev]
    - `ShardReader` reuses descriptors and uses positional reads. [Main Dev]
    - `ExpertCache` now also supports a global `max_bytes` budget. [Main Dev]
    - Byte-budget pilot recorded in
      `docs/results/expert_cache_20260714_byte_budget.md`. [Main Dev]

16. Added complete single-layer Qwen3.6 MoE resident/streaming correctness. [Main Dev]
    - `run_moe_kernel` covers router-selected routed experts, routing-weighted
      accumulation, shared MLP, shared gate, and final output. [Main Dev]
    - `expert-moe-check` is available through the CLI. [Main Dev]
    - Synthetic and real layer 0 checks are exact-equal at every recorded
      comparison point. [Main Dev]
    - Result files:
      `docs/results/qwen36_moe_layer_correctness_20260714.{json,md}`. [Main Dev]

17. Integrated `ExpertCache` into repeated single-layer MoE execution. [Main Dev]
    - Added `CachedExpertProvider` with raw-byte cache entries and reusable
      decoded tensor views. [Main Dev]
    - Added `compare_moe_cache_paths` and the
      `expert-moe-cache-check` CLI. [Main Dev]
    - Verified per-layer LRU, global byte budget, repeated-forward hits,
      batch-union deduplication, eviction, and resident/cached-streaming
      exact equality on synthetic fixtures and real Qwen3.6 layer 0. [Main Dev]
    - Recorded the nine-condition acceptance matrix in
      `docs/results/qwen36_moe_cache_correctness_20260714.{json,md}`. [Main Dev]

18. Completed Stage 5 I/O coalescing, async prefetch, and multi-layer MoE-only runtime. [Main Dev]
    - Added batch shard-range coalescing and useful/physical/wasted byte
      metrics in `src/sparseflow/loader.py`. [Main Dev]
    - Added in-flight deduplicated prefetch with bounded worker threads,
      failure/wait/read-time metrics, and shared-path overlap boundary. [Main Dev]
    - Added `Qwen36MoeOnlyRuntime`, `compare_multilayer_moe_paths`, and the
      `moe-multi-check` CLI. [Main Dev]
    - Real Qwen3.6 layers 0–1 matched resident execution exactly with no
      prefetch, gap=0 prefetch, and gap=6 MiB prefetch. [Main Dev]
    - Stage 5 result matrix is in
      `docs/results/qwen36_stage5_moe_runtime_20260714.{json,md}`. [Main Dev]

19. Implemented the Stage 6 full Qwen3.6 text-only Python reference runtime. [Main Dev]
    - Added `Qwen36TextRuntime` with tokenizer/chat-template encoding,
      prefill, one-token decode, KV-cache handoff, and greedy generation. [Main Dev]
    - Added `SparseFlowQwenExperts`, replacing routed expert modules in all
      Qwen3.6 layers while keeping official attention, Gated DeltaNet, norms,
      residuals, shared expert, and lm-head modules. [Main Dev]
    - Added `text-generate` and `text-check`; the latter is a failure-reporting
      resident/streaming correctness gate with full-logit fingerprints. [Main Dev]
    - Fixed routed dispatch to match Transformers eager ordering by top-k
      position and then token index. [Main Dev]
    - Real experiment machine, prompt `test`, BF16 eager: resident and
      streaming matched exactly across prefill and three KV-cache decode
      steps, including all four 248,320-way logit SHA-256 values. [Main Dev]
    - Four-token streaming with 16 slots/layer recorded 2591 lookups, 300
      hits, 2291 misses, 1651 evictions, and 14,413,725,696 loaded bytes. [Main Dev]
    - Streaming with two prefetch workers also matched logits exactly and
      completed 80 batches with zero failures and zero wasted bytes. [Main Dev]
    - Transformers CPU defaults to grouped-mm; grouped-mm versus eager is now
      explicitly treated as an arithmetic-backend comparison, not a
      storage-policy correctness comparison. [Main Dev]
    - Raw result and limitations are recorded in
      `docs/results/qwen36_stage6_text_runtime_20260714.{json,md}`. [Main Dev]

20. Completed Stage 7.1 memory-native Qwen3.6 text loading and generation. [Main Dev]
    - Added header-only `MemoryLoadPlan`, meta text-only construction, and
      one-tensor-at-a-time selective materialization. [Main Dev]
    - Replaced all 40 language expert modules before payload loading and
      excluded MTP plus vision checkpoint branches. [Main Dev]
    - Initialization loaded 4.560 GiB resident text weights, read zero expert
      payload bytes, and left zero meta or routed-expert Parameters. [Main Dev]
    - A fresh four-token process peaked at 9.216 GiB RSS with 16 slots/layer,
      while generating `[8160, 579, 264, 7047]`. [Main Dev]
    - Memory-native prefill plus three KV decode logits matched resident eager
      SHA-256 exactly; two-worker prefetch also remained exact. [Main Dev]
    - Result: `docs/results/qwen36_stage7_1_memory_native_20260715.{json,md}`.
      [Main Dev]

## Model Analysis

The first local inspection reported:

```text
routed experts                 60.00 GiB
shared experts                246.00 MiB
routers                        41.16 MiB
attention/linear attention      2.44 GiB
vision                         851.77 MiB
embedding/lm_head               1.89 GiB
other dense                     1.52 GiB
dense resident estimate        6.97 GiB
typical routed expert          6.00 MiB
routed experts per layer       1.50 GiB
one cache slot per layer       240.00 MiB
theoretical cold read/token    1.88 GiB
```

This is a good fit for testing expert streaming and caching. It does not yet
prove that a complete Qwen3.6 generation runtime will be fast or comfortable.

## Key Design Decisions

### Model-independent core

The core should own:

- `ShardIndex`: safetensors header indexing without loading payloads.
- `ModelFootprintAnalyzer`: category sizes, per-layer sizes, and read estimates.
- `TierPlanner`: placement decisions based on RAM, VRAM, disk, and context.
- `ExpertLocator`: logical expert to physical tensor slice mapping.
- `ExpertCache`: CPU/GPU hot tier and LRU behavior.
- `HotTierPolicy`: usage count, heat decay, hysteresis, and promotion/eviction.
- Prefetch and route-trace simulation.

### Qwen3.6 adapter

Qwen3.6 routed experts are stored in fused tensors, principally:

```text
model.language_model.layers.<layer>.mlp.experts.gate_up_proj
model.language_model.layers.<layer>.mlp.experts.down_proj
```

An expert is therefore a byte range inside a tensor, not necessarily one
standalone file. `ExpertLocator` must derive the slice from shape, dtype,
expert count, and the actual tensor layout. This is the first important
correctness boundary before implementing a loader or cache.

### Current non-goals

- Do not promise a production chat server or high-throughput serving.
- Do not claim the current Python reference path is the final low-memory loader.
- Vision inputs, MTP/speculative decoding, quantized kernels, and paged KV are
  outside the current text-only milestone.
- Do not couple the model-independent core API to Qwen3.6 naming rules.

## Validation Performed

The local test command is:

```powershell
$env:PYTHONPATH = (Join-Path (Get-Location) 'src')
python -m unittest discover -s tests -p 'test_*.py'
```

The latest experiment-machine run passed:

```text
Ran 82 tests in 3.240s
OK
```

The model was inspected without loading tensor payloads. The analysis is
based on safetensors headers and therefore does not require the full model in
RAM.

The new locator tests use synthetic fused safetensors tensors and cover:

- per-expert byte offsets for both routed parts;
- expert id bounds;
- rejection of a non-contiguous expert axis.

The real-model probe succeeded on the experiment machine:

```text
model       /root/workspace/SparceFlow/model/Qwen3.6-35B-A3B
layer       0
expert      17
gate_up     shape [1024, 2048], offset 1155533600, bytes 4194304
down        shape [2048, 512], offset 36023120, bytes 2097152
total       6291456 bytes
sha256      a5222fdcdff4d504d653e58827d47b8a92f6f8e9663aa48d7738790beaa2572b
```

This confirms the first Qwen3.6 fused-expert layout assumption and proves that
one expert can be read without loading the complete fused tensors. [Main Dev]

The real single-expert kernel check also passed:

```text
layer 0, expert 17, BF16
resident_vs_streaming exact_equal: true
max_abs_error: 0.0
max_rel_error: 0.0
```

The complete single-layer MoE resident/streaming check also passed on the
experiment machine:

```text
command       sparseflow expert-moe-check <model> --layer 0 --rows 2 --seed 1234 --json
top-k         8
unique routed experts 16
resident I/O  1,610,612,736 bytes (1.50 GiB), 2 reads
stream I/O    100,663,296 bytes (96 MiB), 32 reads
selected experts       exact_equal=true
routing weights        exact_equal=true
router logits          exact_equal=true
routed output          exact_equal=true
shared output          exact_equal=true
final output           exact_equal=true
all max abs errors     0
all max rel errors     0
```

The raw result is tracked in
`docs/results/qwen36_moe_layer_correctness_20260714.json`; the method and
limitations are in
`docs/results/qwen36_moe_layer_correctness_20260714.md`. This is a single-MoE-
layer correctness probe, not a claim that the complete Qwen3.6 transformer
runtime is implemented. [Main Dev]

An independent comparison against Transformers'
`Qwen3_5MoeSparseMoeBlock` using the same layer 0 weights and hidden states
also returned `exact_equal=true`, `max_abs_error=0`, and `max_rel_error=0`.
The raw reference record is
`docs/results/qwen36_moe_transformers_reference_20260714.json`. This still
only validates the MoE block, not the surrounding Qwen3.6 transformer. [Main Dev]

The route-trace v2 captures cover Chinese, English, code, math, and continued
conversation prompts at 8, 16, and 32 generated tokens. [Main Dev]

The Stage 6 full text-only runtime correctness gate passed on the remote
experiment machine:

```text
command       sparseflow text-check <model> --dtype bf16 --prompt test
              --max-new-tokens 4 --cache-slots 16 --json
expert math   eager on both resident and streaming
input tokens  11
tokens        [8160, 579, 264, 7047] -> "Here's a thinking"
logits        exact SHA-256 equality for prefill + three decode steps
stream cache  2591 requests, 300 hits, 2291 misses, 1651 evictions
loaded        14,413,725,696 bytes
prefetch      separate exact-logit check: 80 completed batches, 0 failures,
              3186 ranges, 0 wasted bytes
```

The three generated decode tokens used the returned and updated Transformers
KV cache. The full method, raw JSON, and limitations are in
`docs/results/qwen36_stage6_text_runtime_20260714.{md,json}`. This proves
full-flow integration, not final memory efficiency or formal throughput.
[Main Dev]

The remote model copy was then validated in full:

```text
26 safetensors shards
1045 tensors
71,903,776,776 weight bytes (66.97 GiB)
config SHA256 93a4693fa9d8392fbfccd4b3c9873f4bfdcb14fdede978b123d07d19675efe99
index SHA256  41b9356101ebf8e7519e150dc811f80c4226e727301fbb032b890f006ed0be83
all safetensors header-derived sizes matched the actual files
no .incomplete download files remained
```

The current experiment machine was observed as:

```text
Intel Xeon Gold 6248R, 10 physical cores / 20 logical CPUs
125 GiB RAM, no swap, one NUMA node
2 x Tesla V100S 32GB
```

A text-generation smoke test succeeded with Transformers main
(`5.14.0.dev0`, commit `11ed2ff4df5fdfb3117f0e3365ef6ad94081ba69`),
PyTorch `2.9.1+cu128`, FP16, two V100S GPUs, and CPU offload:

```text
input tokens        18
generated tokens     8
model load          21.198 s
generation          18.475 s
decode throughput    0.433 tok/s
```

This is a functionality smoke test only and is not a formal CPU benchmark.

The first materialized CPU Full Resident smoke test succeeded:

```text
dtype              BF16
threads            10
input tokens        18
generated tokens     8
model load          15.960 s
materialization     12.992 s
peak RSS            71.90 GiB
legacy end-to-end generation throughput 1.5212 tok/s
```

The first Colibri-style quality smoke also succeeded on one question with
three choices:

```text
accuracy            1.0
acc_norm_char       1.0
acc_norm_token      1.0
choice forwards     3.628 s
```

That quality smoke's CPU-resident load phase was corrected to `191.829 s` and
reported about `28.26 GiB` of process reads, while the earlier performance
smoke loaded in `15.960 s`. The old `380.486 s` field double-counted
materialization. This is an observed page-cache/cold-start difference, not a
model or backend difference; formal reports must label cache state explicitly.

These are framework validation runs, not formal benchmark results. [Benchmark]

## Current Objective

Stage 7.6 is complete. The final runtime uses grouped native prefill plus the
canonical batch-one W8A8 decode path. Native ABI/leases, offline row sums,
operator profiling, observer cleanup, exact resident/streaming correctness and
the clean-commit warm/cold matrix are recorded in
`docs/results/qwen36_stage7_6_native_critical_path_20260718.md`. Pure fused
decode, lm_head rewrite, deterministic I/O overlap and DeltaNet projection
fusion are retained as measured no-go decisions rather than defaults. [Main Dev]

Stage 7.5.0 is complete. O(1) summary counters, deferred policy diagnostics,
exact decoded-view eviction, no-prefetch fast paths, direct writable reads, and
detailed timing categories are implemented. On the clean C3-R observer matrix,
summary telemetry changed paired decode throughput by `-0.37%`, all 13 runs
kept logits/IDs exact, and the detailed critical path closed to `98.73%`. Evidence:
`docs/results/qwen36_stage7_5_0_observer_20260716.md`. [Main Dev]

Stage 7.5.1 is complete. O(1) LRU/counters, exact decoded-view eviction,
no-prefetch bypass, direct writable reads, and a bounded synchronous BufferPool
raised the frozen S1-8G result from `0.8263` to `1.0763 tok/s` (`+30.3%`) while
median cache accounting, `701.61 MiB/token`, routes, logits, and IDs remained
unchanged. Evidence:
`docs/results/qwen36_stage7_5_1_bf16_hot_path_20260716.md`. [Main Dev]

Stage 7.5.2 is complete. The versioned `canonical-int8-v1` container converted
all 40 layers/10,240 routed experts from 60.000 GiB BF16 to 30.078 GiB physical
storage with 630.84 MiB peak conversion RSS. Full layer-checksum resume,
alignment, index, and fixed dequantization samples passed. The artifact is
ignored under `.cache/stage7_5/qwen36-int8`; evidence:
`docs/results/qwen36_stage7_5_2_int8_container_20260716.md`. [Main Dev]

Stage 7.5.3 is complete. INT8 resident/streaming use identical quantized
weights, scales, reference dequantization, and BF16 expert kernel. The 32-token
gate kept all logits/routes/IDs exact; 4 GiB streaming used 9.91 GiB current RSS
and 335.20 MiB/decode token. BF16/INT8 teacher-forced logits and toy choice
scoring are recorded; standard task manifests remain Stage 7.5.6 Benchmark
work. Evidence: `docs/results/qwen36_stage7_5_3_int8_reference_20260716.md`.
[Main Dev]

Stage 7.5.4 is complete. A canonical-row-major AVX-512 VNNI W8A8 expert
kernel now serves resident and streaming paths without miss-time weight
prepack. The 32-token storage gate remained exact. Three paired runs measured
`1.270x` resident and `1.241x` streaming decode speedup over W8A16 reference;
the independent W8A16-to-W8A8 quality boundary kept 32/32 argmax equality and
no greedy divergence. Evidence:
`docs/results/qwen36_stage7_5_4_native_int8_20260716.md`. [Main Dev]

Stage 7.5.5 is complete. Canonical INT8 now uses the shared cache/prefetch
lifecycle. A 28-cell independent-process matrix passed exact quality, budget,
demand-accounting, and failure gates. S1 LRU won every 1-8 GiB warm budget,
the 4 GiB model-cold cell, and both short-output comparisons; prefetch remains
disabled by default. Evidence:
`docs/results/qwen36_stage7_5_5_int8_cache_prefetch_20260716.md`. [Main Dev]

Stage 7.5.6 and the complete Stage 7.5 plan are complete. The frozen formal
matrix covered BF16/W8A16/W8A8 resident and streaming plus three final cold
replicates. Standard 60-question HellaSwag/ARC/MMLU quality retained exact
native resident/streaming token log-likelihoods. All machine completion gates
passed. Evidence: `docs/results/qwen36_stage7_5_6_formal_20260716.md`.
[Main Dev]

The separate Benchmark workstream may continue Colibri-style
HellaSwag/ARC/MMLU capability evaluation. That task does not reopen the Stage
7.4 storage-policy conclusion: C3-R/C3-S already passed exact 32-step BF16
logits, routes, IDs, budgets, and accounting on the formal workload. [Main Dev]

## Next Work

### Benchmark track

1. Preserve the completed Stage 7.4 raw results and report contract.
2. Add the frozen HellaSwag/ARC/MMLU data manifest and run each task with
   the quality ladder: 1 question per task for Smoke, 3 per task for Pilot,
   5 per task for Development, and 20 per task for the later Formal run.
3. Keep all raw JSON and per-question log-likelihoods for later comparison.
4. Re-run the same quality manifests for INT8 resident and streaming after
   Stage 7.5 is implemented; do not compare different quantization levels as
   a streaming effect.
[Benchmark]

### 2026-07-13 — Reduced Colibri-style quality-eval ladder

- Decided not to use 40 questions per task as the default CPU development run;
  it is too expensive while the backend and cache behavior are still changing.
- Defined the staged sample sizes as: Smoke `1/task` (3 total), Pilot
  `3/task` (9 total), Development `5/task` (15 total), and Formal `20/task`
  (60 total).
- The question IDs, order, and `seed=1234` remain frozen at every stage, and
  raw per-question/per-choice log-likelihoods must be retained.
- A separate n=40 compatibility run may be added later if strict comparison
  with Colibri is needed; it is not part of the default development matrix.
[Benchmark]

### Main Dev track (parallel, informational)

Main Dev remains responsible for:

- validating the implemented `ExpertLocator` against the real Qwen3.6
  safetensors headers;
- running and validating `expert-stat` / `expert-load` against the remote model;
- route schema v2 and phase-aware batch-union measurements are implemented;
- persistent-descriptor loader I/O is implemented; controlled cold-read
  measurement remains;
- complete single-MoE-layer resident/streaming correctness and formal
  ExpertCache integration are implemented;
- full Qwen3.6 text-only Python reference integration is implemented,
  including attention/DeltaNet delegation, KV-cache decode, greedy generation,
  and routed ExpertCache streaming;
- Stage 7.1 memory-native loading is implemented and validated below 10 GiB
  streaming peak RSS; Stage 7.2 C3-R/C3-S same-kernel runtime is complete;
- Transformers' `Qwen3_5MoeSparseMoeBlock` numerical comparison is complete;
  expand correctness across rows, layers, repeated experts, and formal cache
  integration;
- global byte-budget cache mode is implemented; larger budget sweeps remain;
- adding hot-expert policy, predictive prefetch, and platform-specific async I/O;
- adding BF16/INT8/INT4 decode and native expert/attention kernels only after
  correctness gates and profiling freeze their interfaces;
- Stage 7.3 telemetry/cache/prefetch and Stage 7.4 formal Benchmark are
  complete; keep the Stage 7.4 interfaces and raw result contract frozen;
- Stage 7.5 is complete. Preserve the frozen results and interfaces; any next
  stage should begin from the formal bottlenecks rather than modifying Stage
  7.5 evidence. [Main Dev]
- Stage 7.6 Native Decode Critical Path is complete. Preserve the hybrid
  dispatch, clean formal matrix and stop/go evidence. The next implementation
  stage is Stage 7.7 multi-request batching feasibility; it must not reopen
  dense-kernel rewrites without new profiling. [Main Dev]
- Stage 7.7 plan is complete. Execution is strictly route-union analysis,
  real-hidden grouped-kernel gate, shared-cache union replay, and only then a
  fixed-cohort scheduler. The main workload is decode-only; storage exactness,
  scheduler equivalence, aggregate throughput, TPOT, I/O, and fairness have
  separate contracts. Full specification: `docs/stage7_7_plan.md`. [Main Dev]
- Stage 7.7 feasibility gates are complete. Twenty independent real sessions
  showed route overlap, and the real hidden grouped operator was exact with
  B=4 aggregate speedup `1.707x`. Raw INT8 cache replay failed the shared
  streaming normalized-read gate at 4 GiB: union loaded `32.422/122.448 GiB`
  for B=4/B=8 versus round-robin `32.049/101.597 GiB`. The 8 GiB cells were
  near-neutral. Decision: `resident-only-scheduler`; do not implement the
  streaming scheduler until cache-aware cohort formation is designed. Evidence:
  `docs/results/qwen36_stage7_7_multi_request_20260722.md` and
  `benchmarks/results/2026-07-22/stage7_7/summary.json`. [Main Dev]
- exposing timing, RSS, I/O, cache, route, and correctness metrics required by
  `docs/benchmark_setting.md`.

Benchmark will evaluate those implementations through the same frozen
manifests and must not modify the core runtime merely to improve a score.

## Important Risks

- Qwen3.6 combines Gated DeltaNet, Gated Attention, MoE, Vision, and MTP;
  complete runtime implementation is substantially harder than a plain
  Transformer MoE.
- The `6.00 MiB` expert estimate does not capture every layout, alignment,
  quantization, or kernel cost.
- The theoretical `1.88 GiB` cold read per token makes disk bandwidth and
  cache hit rate central performance variables.
- 128GB RAM is suitable for analysis and streaming prototypes, but does not
  imply comfortable generation speed.
- Verify model and dependency licenses before distributing weights, converted
  artifacts, or model-specific adapters.
- The current CPU lacks native BF16 instructions; BF16 is primarily the clean
  correctness matrix, while practical CPU performance will likely require an
  INT8 matrix using AVX-512 VNNI.
- Recently downloaded/loaded model pages may remain in Linux page cache. Any
  uncontrolled read result must not be described as disk-cold performance.
- Colibri-style zero-shot multiple-choice log-likelihood does not let a
  reasoning model use its normal thinking process. Use it for backend quality
  regression, not as the headline measure of Qwen3.6 capability.

## Operational Constraints

- Do not commit model weights, downloaded caches, virtual environments, or
  remote credentials.
- Commit benchmark result documents and raw result JSON to Git so the local
  machine, experiment machine, and GitHub share the same evidence. [Main Dev]
- Keep large datasets, model artifacts, and downloaded caches outside Git.
- Keep local caches and temporary files on `E:`; keep remote caches on the
  large data disk.
- Prefer `uv` for Python environments and dependency installation.
- Add a small reproducible test for each new CLI or core algorithm.
- Record real hardware, filesystem, latency, cache-hit, and read-volume data.
- Keep benchmark manifests immutable once formal comparisons begin; changes
  require a new manifest hash and result series.
- Preserve per-question log-likelihoods and raw per-run metrics, not only
  aggregate scores.
- Update this file after each meaningful experiment or design decision.

## Timeline

### 2026-07-13 — Colibri study and project direction

- Reviewed `JustVugg/colibri` as the primary architectural reference.
- Identified the borrowable ideas: header-only model inspection, tier planning,
  expert streaming, per-layer cache, hot-tier policy, prefetch, route union,
  and an OpenAI-compatible boundary.
- Decided that the reusable contribution is the resource-management layer,
  not a one-model reproduction of Colibri.
- Chose SparseFlow as the project name to keep the scope broader than Qwen.

### 2026-07-13 — Qwen3.6 candidate and local constraints

- Selected `Qwen/Qwen3.6-35B-A3B` as the first experiment target because it is a
  sparse MoE with a relatively small active parameter count.
- Confirmed that local RAM and VRAM are insufficient for a comfortable full
  model run, so the first milestone moved to analysis and streaming rather
  than end-to-end generation.
- Downloaded the model through ModelScope to the ignored local `model/` path.
- Measured the model footprint and identified fused expert tensors as the main
  adapter-specific challenge.

### 2026-07-13 — Inspect/plan implementation

- Implemented header-only safetensors indexing.
- Added tensor classification, footprint analysis, and tier planning.
- Added `inspect` and `plan` CLI commands.
- Added two unit tests; both passed.
- Wrote `docs/implementation_plan.md` with the staged implementation path.

### 2026-07-13 — Remote experiment strategy

- Decided to rent a large-memory CPU machine for the first real experiment.
- Preferred approximately 128GB RAM plus 300GB or more of fast storage over an
  expensive GPU-first setup.
- The first remote milestone is to measure expert slicing, disk streaming, and
  cache behavior. GPU hot-tier experiments can follow later.
- Began installing Codex on the remote machine so experiments can be performed
  in the same project context.

### 2026-07-13 — Public repository established

- Created the public GitHub repository `GoDiao/SparseFlow` with `gh`.
- Pushed the initial `main` branch.
- Added this handoff document to make the project state recoverable for future
  agents and remote sessions.

### 2026-07-13 — Experiment machine ready

- Main Dev connected the local workspace to `origin/main` at
  `https://github.com/GoDiao/SparseFlow.git`.
- The Qwen3.6 model was downloaded to the remote experiment machine.
- The project is ready to begin the next implementation milestone:
  `ExpertLocator`, followed by `expert-stat` and `expert-load`.
- The first remote measurements must record the exact model path, filesystem
  capacity, and cold/warm expert read latency.

### 2026-07-13 — Benchmark workstream established

- The active workstream identity changed from Main Dev to Benchmark for the
  current session; Main Dev remains the separate owner of runtime/core work.
- Verified all 26 model shards, 1045 tensors, and 66.97 GiB of safetensors
  payload on the experiment machine.
- Installed a Transformers main revision that recognizes the internal
  `qwen3_5_moe` architecture and completed an 8-token text-generation smoke
  test with GPU+CPU offload.
- Decided to focus the formal comparison on CPU Full Resident, Generic
  Offload, SparseFlow Resident, and SparseFlow Streaming modes.
- Split evaluation into performance, numerical correctness, greedy regression,
  and Colibri-style multiple-choice log-likelihood quality tests.
- Added `docs/benchmark_setting.md` as the canonical experiment matrix and
  result-contract document.
[Benchmark]

### 2026-07-13 — Benchmark framework smoke validation

- Added `benchmarks/run_cpu.py`, `benchmarks/score_choices.py`, fixed prompt
  manifests, and benchmark usage documentation.
- Corrected C1 semantics by materializing mmap-backed tensors into anonymous
  CPU memory before labeling a run Full Resident.
- CPU BF16 materialized smoke: 10 threads, 18 input tokens, 8 generated
  tokens, peak RSS 71.90 GiB, legacy end-to-end generation throughput
  1.5212 tok/s; this is not the corrected steady-state decode metric.
- Colibri choice-scoring smoke: one question, three choices, accuracy and both
  normalized scores equal to 1.0; the corrected total load phase was
  191.829 s under a cold page-cache condition, including 188.657 s of
  materialization. The earlier 380.486 s field double-counted materialization.
[Benchmark]

### 2026-07-14 — Corrected benchmark timing semantics

- Fixed `benchmarks/score_choices.py`: `load.seconds` now contains the total
  tokenizer/model/materialization interval exactly once; tokenizer load,
  model load, and materialization are recorded separately.
- Replaced the CPU runner's end-to-end `model.generate()` timing with an
  explicit greedy loop. The first forward is reported as prefill/TTFT, later
  one-token forwards as steady-state decode, and the full interval as
  `end_to_end_seconds`.
- Renamed the headline CPU speed fields to
  `decode_tokens_per_second` and `end_to_end_tokens_per_second`; the old
  `generation_tokens_per_second` value must not be called decode throughput.
- Bumped the executable benchmark result schema to version 2.
[Benchmark]

### 2026-07-13 — Record identity policy

- Decided that every new implementation record, experiment record, and design
  decision must end with an explicit executor identity marker such as
  `[Main Dev]` or `[Benchmark]`.
[Benchmark]

### 2026-07-13 — Main Dev ExpertLocator implementation [Main Dev]

- Main Dev implemented `src/sparseflow/locator.py` for contiguous fused expert
  slices. [Main Dev]
- Added `tests/test_locator.py` with synthetic safetensors coverage. [Main Dev]
- Local validation now passes 5 tests. [Main Dev]
- Real-model validation is pending on the experiment machine; the next runtime
  milestone is phase-aware route replay and single-layer correctness. [Main Dev]

### 2026-07-13 — Main Dev expert probe commands [Main Dev]

- Added `expert-stat` for metadata-only fused expert location. [Main Dev]
- Added `expert-load` for exact-range raw reads and SHA-256 verification. [Main Dev]
- Synthetic and real Qwen3.6 CLI validation both pass; layer 0, expert 17 was
  read successfully on the experiment machine. [Main Dev]

### 2026-07-13 — Main Dev real expert probe validation [Main Dev]

- Ran `expert-stat` against the full Qwen3.6 model for layer 0, expert 17. [Main Dev]
- Confirmed axis-0 fused layout for `gate_up_proj` and `down_proj`. [Main Dev]
- Ran `expert-load` and read exactly 6.00 MiB from two shards. [Main Dev]
- Recorded the combined SHA-256 for repeatability. [Main Dev]

### 2026-07-13 — Main Dev ExpertCache pilot [Main Dev]

- Implemented per-layer LRU cache and `expert-bench`. [Main Dev]
- Ran a four-layer, 128-request Qwen3.6 locality pilot at 0/1/2/4 slots per
  layer. [Main Dev]
- Observed hit rates from 0.00% to 4.69% and loaded bytes from 768 MiB to
  732 MiB. [Main Dev]
- Stored the detailed method and results in
  `docs/results/expert_cache_20260713_qwen36_pilot.md`. [Main Dev]
- Marked the run as warm-page-cache; it is not a cold SSD benchmark. [Main Dev]

### 2026-07-13 — Main Dev long traces and real routing [Main Dev]

- Ran 512-request uniform and locality comparisons at five cache capacities. [Main Dev]
- Added `route-trace` using the actual Qwen3.5-MoE router's
  `selected_experts` output. [Main Dev]
- Captured and replayed a real Qwen3.6 trace with 2880 requests. [Main Dev]
- Stored the comparison, raw result references, and limitations in
  `docs/results/expert_cache_20260713_long_comparison.md`. [Main Dev]

### 2026-07-13 — Result versioning policy [Main Dev]

- Decided that benchmark result documents and raw result JSON should be
  tracked in Git and synchronized through GitHub. [Main Dev]
- Kept model weights, caches, and large datasets excluded from Git. [Main Dev]

### 2026-07-14 — Main Dev route schema and correctness milestones [Main Dev]

- Upgraded route traces to schema v2 with explicit prefill/decode and token
  row grouping. [Main Dev]
- Captured five prompt categories at 8, 16, and 32 generated tokens. [Main Dev]
- Added batch-union and per-phase cache accounting; real v2 replay reports
  prefill 2560 raw / 1468 effective requests and decode 320 requests. [Main Dev]
- Added `expert-kernel-check`; real layer 0, expert 17 resident/streaming
  output comparison is exactly equal. [Main Dev]
- Added persistent `ShardReader` positional reads and global byte-budget cache
  support. [Main Dev]
- Detailed results are in
  `docs/results/expert_cache_20260714_route_trace_v2.md` and
  `docs/results/route_trace_v2_long_capture_20260714.md`. [Main Dev]

### 2026-07-14 — Main Dev loader and byte-budget milestones [Main Dev]

- Replaced per-part open/seek/read with persistent `ShardReader` positional
  reads and per-capacity reader metrics. [Main Dev]
- Added `expert-kernel-check` and validated real Qwen3.6 layer 0, expert 17
  with exact resident/streaming equality. [Main Dev]
- Added global byte-budget cache mode and recorded a 512 MiB / 1 GiB pilot in
  `docs/results/expert_cache_20260714_byte_budget.md`. [Main Dev]

### 2026-07-14 — Main Dev complete single-layer MoE correctness [Main Dev]

- Implemented `compare_moe_paths` and the shared `run_moe_kernel` in
  `src/sparseflow/moe_probe.py`. [Main Dev]
- Added Qwen3.6 router, routed expert, shared expert, and shared gate tensor
  loading with dtype/shape validation. [Main Dev]
- Added `expert-moe-check` to the public CLI. [Main Dev]
- Added synthetic full-MoE and CLI tests; the full suite now passes 16 tests.
  [Main Dev]
- Ran the real experiment machine's Qwen3.6 layer 0 with two rows and top-8
  routing. Resident and streaming paths matched exactly for selected experts,
  routing weights, router logits, routed output, shared output, and final
  output; every recorded max absolute and relative error was zero. [Main Dev]
- Resident preloaded 1.50 GiB of routed weights; streaming read 16 unique
  selected experts, 96 MiB total. This is a correctness/storage probe, not a
  complete generation runtime or a cold-disk throughput result. [Main Dev]
- Stored the raw result and method in
  `docs/results/qwen36_moe_layer_correctness_20260714.{json,md}`. [Main Dev]
- Independently compared the same implementation to Transformers'
  `Qwen3_5MoeSparseMoeBlock`; the result was also exact equal with zero
  absolute and relative error. [Main Dev]
- Strengthened the synthetic fixture to top-2 routing across multiple rows,
  which exercises repeated selected experts and verifies that streaming reads
  each unique expert once. [Main Dev]

### 2026-07-14 — Main Dev ExpertCache MoE integration [Main Dev]

- Replaced the single-layer probe's temporary streaming expert dictionary with
  `CachedExpertProvider` backed by the formal `ExpertCache`. [Main Dev]
- Kept raw payload storage model-independent and reused decoded tensor views
  while the corresponding cache entry remained live. [Main Dev]
- Added repeated-forward execution and the `expert-moe-cache-check` CLI. [Main Dev]
- Real Qwen3.6 layer 0, 8 rows, 4 forwards, 64 slots/layer: 226 cache
  requests, 123 hits, 103 misses, 39 evictions, 648,019,968 loaded/read
  bytes, and exact resident/cached-streaming outputs. [Main Dev]
- Real 4-slot LRU and 6 MiB global-budget runs both produced actual
  evictions, respected their cache limits, matched reader bytes to loaded
  bytes, and remained exact equal to resident execution. [Main Dev]
- Synthetic tests now cover repeated expert reuse, per-layer isolation,
  eviction, byte budget, and the new CLI; the suite passes 17 tests. [Main Dev]
- Result matrix: `docs/results/qwen36_moe_cache_correctness_20260714.{json,md}`.
  [Main Dev]

### 2026-07-14 — Main Dev Stage 5 runtime and I/O [Main Dev]

- Added `coalesce_locations` and `ShardReader.read_locations`; batch reads now
  split physical ranges back into exact logical expert parts. [Main Dev]
- Added bounded asynchronous prefetch with in-flight deduplication and
  useful/physical/wasted byte plus read/wait timing metrics. [Main Dev]
- Added `Qwen36MoeOnlyRuntime` for multi-layer MoE execution and
  `moe-multi-check`. [Main Dev]
- Real Qwen3.6 layers 0–1, 2 rows, 16 slots/layer: 32 cache requests, 30
  prefetch-consumed hits, 2 misses, 201,326,592 useful expert bytes, and
  exact resident/streaming outputs. [Main Dev]
- With `coalesce_gap=0`, routed reads were reduced from 64 to 62 without
  physical-byte waste; with a 6 MiB gap, reads fell to 58 at the cost of
  20,971,520 wasted bytes. Both remained exact equal. [Main Dev]
- Synthetic and real tests pass; the suite now has 20 passing tests, including
  prefetch failure cleanup. [Main Dev]
- Result matrix: `docs/results/qwen36_stage5_moe_runtime_20260714.{json,md}`.
  [Main Dev]

### 2026-07-14 — Writer workstream started [WRITER]

- Confirmed that Writer is a role/workstream identity, not a separate Git
  branch; the repository remains on `main` and Main Dev retains runtime
  ownership. [WRITER]
- Added `docs/content_series_plan.md` with a nine-stage SparseFlow Build in
  Public storyline, platform adaptation rules for Xiaohongshu/X, and evidence
  references. [WRITER]
- The nine stages cover the resource-constrained starting point, Colibri
  research, model inspection, ExpertLocator, route/cache analysis,
  correctness, ExpertCache, I/O/prefetch/multi-layer runtime, and the future
  full Transformer/open-source evaluation path. [WRITER]

### 2026-07-14 — Main Dev Stage 6 text-only runtime [Main Dev]

- Added the full Python reference integration around Transformers' Qwen3.6
  text model; SparseFlow owns routed expert lookup, raw-byte cache, decode
  views, prefetch, and metrics. [Main Dev]
- Added `text-check`, which loads resident and streaming sequentially and
  fails on any input/token/text/full-logit fingerprint mismatch. [Main Dev]
- Found that Transformers defaults to grouped-mm on CPU while SparseFlow is
  eager; fixed the correctness gate to use eager on both sides and kept
  grouped-mm as a separately labeled performance backend. [Main Dev]
- Aligned routed dispatch order with the official eager implementation. [Main Dev]
- Verified exact 4-step full-vocabulary logits for prefill plus three KV-cache
  decode forwards; both paths produced `[8160, 579, 264, 7047]`. [Main Dev]
- Verified full-runtime prefetch with exact logits: two workers completed 80
  batches, zero failures, 1851 submitted experts, and no coalescing waste. [Main Dev]
- Stage 6 remains a correctness/reference milestone. The first implementation
  still loads the checkpoint before replacing routed modules, so the final
  low-peak-memory loader is Stage 7 work. [Main Dev]

### 2026-07-15 — Main Dev Stage 7.1 memory-native runtime [Main Dev]

- Added `native-plan`, `native-meta`, and `native-load`, plus memory-native
  modes for `text-generate` and `text-check`. [Main Dev]
- Classified 4.560 GiB as text resident, 60.000 GiB as streamed language
  experts, and skipped 1.573 GiB MTP plus 0.832 GiB vision. [Main Dev]
- Built the causal text model on meta, installed 40 streaming expert modules,
  and selectively loaded all 613 resident tensors with zero expert reads.
  [Main Dev]
- Fresh-process four-token generation peaked at 9.216 GiB RSS; no full expert
  Parameter or unresolved meta tensor existed. [Main Dev]
- Full logits matched eager resident exactly across prefill and three KV-cache
  decode steps; async prefetch also passed exact correctness. [Main Dev]
- Evidence: `docs/results/qwen36_stage7_1_memory_native_20260715.{json,md}`.
  [Main Dev]

### 2026-07-15 — Main Dev Stage 7.2 C3-R / C3-S runtime [Main Dev]

- Added the formal `ExpertProvider` boundary and made resident/streaming
  backends feed the same `SparseFlowQwenExperts`, routed dispatch, and eager
  BF16 expert kernel. [Main Dev]
- C3-R preloaded 40 layers, 10,240 logical experts, and 80 fused buffers:
  exactly 64,424,509,440 bytes (60.000 GiB). Expert I/O remained zero after
  preload. [Main Dev]
- Added `--expert-backend sparseflow-resident|sparseflow-streaming` and the
  `runtime-check` correctness/invariant gate. [Main Dev]
- Real Qwen3.6 prefill plus three KV-cache decode forwards produced exact
  full-logit fingerprints, 160 exact route digests, identical IDs
  `[8160, 579, 264, 7047]`, and identical text. [Main Dev]
- Independent Chinese, Python-code, and math prompts each passed two-token
  full-logit, route, identity, and storage-invariant checks. [Main Dev]
- Fresh one-token processes peaked at 66.364 GiB for C3-R and 9.205 GiB for
  C3-S. The combined correctness run is warm-page-cache evidence, not the
  formal Stage 7.4 benchmark. [Main Dev]
- Evidence: `docs/results/qwen36_stage7_2_c3_runtime_20260715.{json,md}`.
  [Main Dev]

### 2026-07-15 — Main Dev Stage 7.3 cache and prefetch runtime [Main Dev]

- Added S0-S4 `CachePolicy` variants with no-cache, LRU, bounded hot tier,
  heat decay, hysteresis, phase-aware admission, and global byte budgets.
  [Main Dev]
- Added per-forward/per-layer telemetry for routes, cache deltas, demand I/O,
  async prefetch, wall intervals, and memory state; JSONL rows carry explicit
  Main Dev attribution. [Main Dev]
- Added bounded current-route and three-route-stability predictive prefetch,
  in-flight deduplication, transient payload reuse, cancellation/finalization,
  and useful/late/wasted accounting. [Main Dev]
- Replayed 15 real 8/16/32-token traces over 1/2/4/8 GiB and S0-S4: all 300
  runs respected budgets and accounting invariants. No single policy won every
  budget, so Stage 7.4 must keep the full attribution matrix. [Main Dev]
- Real 8-token Qwen3.6 S0-S4 runs all matched C3-R full logits, 320 routes,
  IDs/text, and runtime identity exactly. At 4 GiB, S3 and S4 both read
  15.79 GiB; S4 served 2693 requests through prefetch with one sync miss and
  zero duplicate read/waste. [Main Dev]
- A 1 GiB exact run exercised 45 stable-route predictions with zero failures;
  84 MiB remained unused, so predictive prefetch is not promoted as the small-
  budget default. [Main Dev]
- Evidence: `docs/results/qwen36_stage7_3_cache_prefetch_20260715.md` and its
  linked JSON/JSONL files. [Main Dev]

### 2026-07-15 — Main Dev Stage 7.4 formal Benchmark [Main Dev]

- Added the Stage 7.4 C3 runner, 27-cell independent-process matrix, generic
  Accelerate offload baseline, thread calibration, buffered expert I/O probe,
  strict summarizer, and system report builder. [Main Dev]
- Froze 10 CPU threads after 1/5/10/20 calibration; C3-R reached 2.934 tok/s
  at 10 threads versus 2.837 at 20. Model-cold buffered expert I/O measured
  about 0.161 GiB/s; workload-warm reached 3.257 GiB/s with 8 workers.
  [Main Dev]
- Executed 27 C3 cells over C3-R, S0, S1-S4, 1/2/4/8 GiB, workload-warm,
  and three independent model-cold S0/S3/S4 repeats. Total matrix time was
  about 2.41 hours. [Main Dev]
- Every C3 run used one model revision and clean commit; all 32-step full
  logits, route audits, generated IDs, runtime identity, cache budgets,
  demand accounting, and prefetch-failure gates passed exactly. [Main Dev]
- C3-R measured 2.9618 tok/s at 66.355 GiB RSS. The best current Python warm
  point was C3-S1 LRU 8 GiB: 0.8263 tok/s, 13.651 GiB RSS, and 701.61 MiB
  expert read per decode token. S1 beat the more complex warm S2-S4 variants.
  [Main Dev]
- C3-S0 measured 0.3725 tok/s model-cold and 0.4752 tok/s workload-warm.
  C3-S4 4 GiB cold was 1.17x faster than S3 cold, proving useful synchronous
  I/O overlap even though S4 was not the best warm policy. [Main Dev]
- C1 Transformers resident measured 2.9595 tok/s and 67.074 GiB RSS. Its
  grouped-mm path first diverged from C3-R at generated token index 23, so
  C1/C3 remains a system-level comparison; C3-R/C3-S is the attribution gate.
  [Main Dev]
- Built a complete 1045-tensor, 71,903,645,408-byte Accelerate disk-offload
  layout. Three C2 model-cold samples measured median TTFT 308.8 seconds and
  median decode 0.02567 tok/s; workload-warm measured 0.02794 tok/s. C3-S1
  8 GiB was 29.6x faster than warm C2. [Main Dev]
- The C2 result intentionally uses two generated tokens because cold decode
  ranged from 35.7 to 310.7 seconds and requests read 64.56–128.43 GiB from
  the block device. No synthetic 32-token C2 result was extrapolated.
  [Main Dev]
- Evidence: `benchmarks/results/2026-07-15/stage7_4/report.md`,
  `system_summary.json`, and the tracked `c3_raw/` directory. [Main Dev]

## Handoff Update Protocol

When continuing this project:

1. Read `Active Agent Role`, `Current State`, `Current Objective`, and
   `Next Work` first; do not assume every agent is Main Dev.
2. Read the latest timeline entry before making a new design decision.
3. Run the relevant tests and record the command/result in `Validation
   Performed` or a new dated timeline entry.
4. Append a new dated timeline section after each meaningful milestone.
5. Benchmark must update `docs/benchmark_setting.md` when the experiment
   contract, metrics, manifests, or eval protocol changes.
6. Every newly added implementation record, experiment record, or design
   decision must end with an explicit executor identity marker, such as
   `[Main Dev]` or `[Benchmark]`.
7. Keep this document factual: record observed results separately from plans
   and hypotheses.

### 2026-07-16 — Main Dev Stage 7.5.4 native INT8 runtime [Main Dev]

- Added a canonical-row-major AVX-512 VNNI W8A8 operator with dynamic U8
  activations, S8 weights, row-sum zero-point correction, and no miss-time
  weight prepack. [Main Dev]
- Bound native tensor views to exact cache entries and removed them on
  eviction; resident and streaming invoke the same native expert kernel.
  [Main Dev]
- The real 32-token gate preserved exact routes, full logits, IDs, text,
  cache budget, and demand accounting. [Main Dev]
- Three paired 10-thread runs measured native resident at `2.2483 tok/s`
  (`1.270x` reference) and native 4 GiB streaming at `1.1361 tok/s`
  (`1.241x` reference). Every paired invariant passed. [Main Dev]
- W8A8 versus W8A16 teacher-forced quality retained 32/32 argmax equality,
  no greedy divergence, maximum KL `0.016955`, and 96.25% mean top-10
  overlap. [Main Dev]
- Evidence: `docs/results/qwen36_stage7_5_4_native_int8_20260716.md` and its
  three linked raw JSON files. [Main Dev]

### 2026-07-16 — Main Dev Stage 7.5.5 INT8 cache calibration [Main Dev]

- Reused the formal cache/prefetch lifecycle for canonical INT8 ranges,
  including Future deduplication, transient admission, exact eviction cleanup,
  and useful/late/wasted accounting. [Main Dev]
- A real four-token gate served 2,819 requests through prefetch with three
  synchronous misses, zero failures/waste/transient residue, and exact
  resident/streaming outputs. [Main Dev]
- Completed 28 independent cells over policy, 0.5-8 GiB budgets, warm/cold
  state, workers, coalescing, and 8/16/32-token output lengths. All machine
  validation gates passed. [Main Dev]
- Selected S1 LRU with prefetch disabled as the host default. S1 reached
  `1.0816 tok/s` at 4 GiB and `1.4312 tok/s` at 8 GiB. S4 remains opt-in with
  two workers and a 64 KiB gap as its best measured setting. [Main Dev]
- Evidence: `docs/results/qwen36_stage7_5_5_int8_cache_prefetch_20260716.md`.
  [Main Dev]

### 2026-07-16 — Main Dev Stage 7.5.6 formal completion [Main Dev]

- Completed seven three-run warm performance cells and three independent
  final native model-cold replicates on clean benchmark commits. [Main Dev]
- W8A8 native reached `2.4925 tok/s` resident, `1.0981 tok/s` at 4 GiB, and
  `1.4157 tok/s` at 8 GiB. The 4 GiB path used about 9.62 GiB current RSS and
  read 354.98 MiB per decode token. [Main Dev]
- Froze official HellaSwag/ARC-Challenge/MMLU revisions and nested
  3/9/15/60-row manifests with `seed=1234`. [Main Dev]
- Completed 60-question BF16, native resident, and native streaming Formal
  scoring. Native resident/streaming matched every prediction, choice total,
  and token log-likelihood exactly; BF16/native char-normalized predictions
  matched 60/60. [Main Dev]
- W8A16 reference Formal task scoring was stopped as an explicit reference-
  dequant observer case; its standard Smoke, teacher-forced numerical gate,
  and full performance matrix remain recorded. No missing value was imputed.
  [Main Dev]
- All Stage 7.5.6 machine gates passed. Evidence:
  `docs/results/qwen36_stage7_5_6_formal_20260716.md`. [Main Dev]

### 2026-07-18 — Main Dev Stage 7.6 native critical path complete [Main Dev]

- Added operator-level runtime/native profiling, offline row-sum execution
  metadata, cache leases and the stable `NativeExpertBatch` boundary. [Main Dev]
- Added fused grouped MoE prefill and canonical batch-one decode dispatch.
  Pure fused decode measured `0.886x` legacy in same-process AB/BA and is not
  the default; hybrid retained exact outputs and prefill gains. [Main Dev]
- The real 32-token resident/streaming gate kept all logits, 1,280 routes,
  IDs/text, cache budget and demand accounting exact; all leases released and
  resident generation expert I/O remained zero. [Main Dev]
- Summary telemetry now uses generation-boundary provider totals. Nine AB/BA
  pairs found no measured summary slowdown, while host variance was too large
  to resolve an absolute ±1% effect; profile closure was 98.94%. [Main Dev]
- Clean commit `0d4bdf9` formal results were `2.1513 tok/s` resident,
  `1.4128 tok/s` at 4 GiB warm, `1.4944 tok/s` at 8 GiB warm, and `1.0227
  tok/s` for the three-run 4 GiB model-cold median. [Main Dev]
- Grouped prefill and the 4 GiB/cold paths passed their performance goals.
  Resident/8 GiB stretch goals were not met; lm_head, deterministic I/O
  overlap and DeltaNet projection fusion were recorded as no-go decisions.
  [Main Dev]
- Evidence:
  `docs/results/qwen36_stage7_6_native_critical_path_20260718.md` and
  `benchmarks/results/2026-07-18/stage7_6/formal/summary.json`. [Main Dev]
- The machine verifier passed every clean-commit, identity, 32-logit,
  cache-pin, cold-replicate and summary/raw consistency gate. Evidence:
  `benchmarks/results/2026-07-18/stage7_6/formal/verification.json`. [Main Dev]

### 2026-07-22 — Main Dev Stage 7.7 implementation plan frozen [Main Dev]

- Added the complete feasibility-first Stage 7.7 specification covering the
  session-aware trace schema, real-route union analysis, grouped-kernel gate,
  shared-cache replay, fixed-cohort scheduler, formal matrix, and decision
  rules. [Main Dev]
- The plan keeps raw results in Git, large hidden-state fixtures in the ignored
  project cache, and `.handoff.md` as a compact pointer. [Main Dev]
- Stage 7.7 implementation completed the route capture, raw cache replay,
  grouped-kernel benchmark, summarizer, and verifier described by the plan.
  The full scheduler was correctly gated off after the streaming NO-GO.
  Evidence: `docs/stage7_7_plan.md`. [Main Dev]

### 2026-07-22 — Main Dev Stage 7.7 feasibility validation complete [Main Dev]

- Captured 20 independent schema-v3 route sessions and analyzed synchronized,
  offset, locality, and diverse schedules. Synchronous union ratios were
  `1.1902x/1.5185x/1.7492x/2.1690x` for B=2/4/8/16. [Main Dev]
- Replayed B=4/B=8 union and round-robin schedules against raw INT8 data and
  scales using the production ExpertCache and persistent ShardReader. Every
  cell respected its byte budget, released all leases, and had exact demand
  accounting. [Main Dev]
- Captured real decode hidden rows for eight independent prompts. The current
  fused multi-row native operator was exact for B=1/2/4/8; B=4 aggregate
  speedup was `1.707x`, above the `1.5x` gate. [Main Dev]
- The streaming normalized-read gate failed at 4 GiB: union loaded more than
  round-robin for both B=4 and B=8. Stage 7.7 ends as a structured NO-GO for a
  shared streaming scheduler and GO for a bounded resident-only scheduler.
  Result document: `docs/results/qwen36_stage7_7_multi_request_20260722.md`.
  [Main Dev]

### 2026-07-22 — Main Dev Stage 7.8 grouped acceleration validation complete [Main Dev]

- Added `native/grouped_moe.cpp`, `GroupedMoEWorkspace`, and
  `native_dispatch="grouped"`. The operator builds a stable per-expert
  GroupPlan, processes repeated rows under one expert/output-channel task,
  and preserves the original token top-k reduction order. [Main Dev]
- The real hidden/routes operator matrix was exact for B=1/2/4/8. Grouped
  speedups versus canonical were `3.717x/2.116x/1.985x/3.102x`; B=1 was
  `1.004x` versus old fused and B=4 passed the `1.95x` target. [Main Dev]
- Added the fixed resident Qwen cohort runner. Grouped versus old fused batch
  was exact for B=4 and B=8; independent-session IDs, text, and argmax were
  equal. Batched full-vocabulary logits were not bit-exact against independent
  batch-one ATen execution and are recorded separately. [Main Dev]
- Added bounded cache-aware sub-cohort replay. At 4 GiB it did not consistently
  beat Stage 7.7 round-robin; only B=8/8 GiB reduced loaded bytes (`0.99340x`).
  Streaming remains gated and the default dispatch is unchanged. [Main Dev]
- Structured verification passed integrity, exact grouped-vs-fused output,
  resident behavior, byte budgets, accounting, and lease-release checks. The
  summary correctly reports `all_pass=false` with explicit streaming/logit
  no-go reasons. [Main Dev]
- Evidence: `docs/results/qwen36_stage7_8_native_grouped_20260722.md`,
  `benchmarks/results/2026-07-22/stage7_8/summary.json`, and
  `benchmarks/results/2026-07-22/stage7_8/verification.json`. [Main Dev]

### 2026-07-22 — Main Dev Stage 7.8 formal resident acceptance [Main Dev]

- Added `benchmarks/run_stage7_8_formal.py`, which enforces B=1/4/8,
  32-token generation, three same-process ABBA repetitions, equal prompt
  lengths, full-logit comparison, route fingerprints, P50/P95, and runtime
  identity. [Main Dev]
- The formal baseline `3f935f2` passed all harness gates. Grouped/fused full
  logits, routes, IDs, text, argmax, and repeated observations were exact in
  all three batches. [Main Dev]
- Formal resident medians were grouped `1.922/4.257/5.565 tok/s` versus
  hybrid/fused `1.898/3.846/4.852 tok/s` for B=1/4/8; P50/P95 latency is in
  the formal result document. [Main Dev]
- Strengthened `verify_stage7_8.py` with hard protocol, performance-threshold,
  runtime-identity, formal-commit, and current-clean-tree checks. [Main Dev]
- This is an equivalent long-generation quality gate, not a 60-question
  matrix. Independent-session full-logit reduction-order differences remain
  documented, and streaming remains separately gated. [Main Dev]
- Evidence: `docs/results/qwen36_stage7_8_formal_acceptance_20260722.md` and
  `benchmarks/results/2026-07-22/stage7_8/formal_resident_abba.json`. [Main Dev]

### 2026-07-22 — Main Dev Stage 7.9 Public Alpha release hardening complete [Main Dev]

- Added the frozen Public Alpha presets: `stable` maps to INT8 native resident
  hybrid, `low-memory` maps to single-request INT8 native streaming with S1
  LRU and prefetch disabled, and `experimental-batch` maps to resident grouped
  fixed cohorts. Shared streaming batching remains disabled. [Main Dev]
- Added `sparseflow doctor`, which performs header-only model checks, complete
  40-layer INT8 container validation, offline row-sum validation, disk reserve
  checks, AVX-512 VNNI detection, and optional native extension diagnostics.
  [Main Dev]
- Added `sparseflow prepare-int8` for resumable expert conversion plus offline
  execution metadata, and `sparseflow run` for the three public presets. [Main Dev]
- Added `README.md` with source-install Quick Start, supported environment,
  model/cache/temp workflow, failure boundaries, and public path status. [Main Dev]
- Added the compact Stage 7.9 validation ladder. Smoke 32, Standard 128, and
  Endurance 512 all passed resident hybrid vs single-request S1 streaming exact
  IDs, text, routes, fingerprints, and selected in-memory logits. No full logits
  were persisted. [Benchmark]
- Native extension build, model doctor, 4 GiB streaming budget, and all three
  validation verifiers passed on the experiment host. [Benchmark]
- Formal 60-question quality evidence is carried forward from Stage 7.5.6;
  Stage 7.8 formal resident B=1/4/8 evidence remains the grouped experimental
  gate. [Main Dev]
- Plan and results: `docs/stage7_9_plan.md`,
  `docs/results/qwen36_stage7_9_public_alpha_20260722.md`, and
  `benchmarks/results/2026-07-22/stage7_9/summary.json`. [Main Dev]

### 2026-07-22 — Main Dev Stage 7.9 closure fixes local implementation [Main Dev]

- Removed the top-level CLI import of `text_runtime`; runtime commands now use
  lazy optional imports, while `preset`, `inspect`, `plan`, and header-only
  `doctor` remain usable without torch. Missing runtime extras now produce a
  clean installation error. [Main Dev]
- Added cgroup-aware Doctor RAM admission with `MemAvailable`, override,
  required/recommended thresholds, dense/INT8/cache/sidecar/KV/DeltaNet/
  workspace/page-cache components, and `--available-ram`, `--ctx`, and
  `--batch-size` controls. [Main Dev]
- Added closure evidence plan at
  `docs/stage7_9_closure_fixes.md`. Local 99-test, compile, `python -S`, and
  diff gates pass. A clean commit must be pushed before experiment-host
  no-torch and real Doctor/RSS acceptance; formal release evidence remains
  blocked until those checks pass. [Main Dev]

### 2026-07-22 — Main Dev Stage 7.9 closure acceptance complete [Main Dev]

- Pushed clean closure commit `36aa619901561cf4148dbf8dfaadb8f73b058d53`
  using the repository SSH key. The local branch and `origin/main` are equal.
  [Main Dev]
- A fresh no-torch `uv` environment passed `preset`, `inspect`, `plan`, and
  low-memory `doctor`; `run` returned a clean optional-runtime error with no
  traceback. [Main Dev]
- Experiment-host stable Doctor required/recommended `38.69/42.56 GiB`, with
  separate-process peak RSS `36.63 GiB`; low-memory required/recommended
  `12.62/14.62 GiB`, with peak RSS `9.66 GiB`. Both Doctor checks passed using
  cgroup memory source. [Main Dev]
- Smoke 32, Standard 128, and Endurance 512 were regenerated from the closure
  commit. All exactness, 4 GiB budget, compactness, identity, and clean
  provenance gates passed. Evidence is in
  `benchmarks/results/2026-07-22/stage7_9/` and the Public Alpha report.
  [Main Dev]
